# frozen_string_literal: true

module CornerstoneRails
  # The gem's version. It always equals the version of Cornerstone Components it vendors, so the
  # gem and the npm package can be read as one release. See README "Versioning".
  VERSION = "0.6.2"

  # The @cruglobal/cornerstone-components version whose files live in vendor/cornerstone.
  COMPONENTS_VERSION = VERSION
end
